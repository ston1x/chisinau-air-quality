<h2>Chisinau Air Quality</h2>
<h3>Submit a report</h3>
<%=
  form_for :report, routes.create_report_path, values: {report: {status: true}} do
  label 'Air quality'
    div class: 'form-field' do
      radio_button :status, true, class: "form-check"
      label 'stinky'
      radio_button :status, false, class: "form-check"
      label 'good'
    end

    div class: 'form-field' do

      label 'Neighbourhood'
      select :location, neighbourhoods
    end
    submit 'Submit report', class: 'btn btn-primary'
  end
%>

<h3>Reports</h3>
<% if reports.any? %>
  <div id="reports">
  <% reports.each do |report| %>
    <div class="report">
      <p>
      <%= report&.created_at&.getlocal('+03:00')&.to_s(:db) %>,
      <%= neighbourhoods.key(report.location) %>,
      <% if report.stinky %>
        stinky 💩
      <% else %>
        fresh air 🍃
      <% end %>
      </p>
    </div>
  <% end %>
  </div>
<% end %>
